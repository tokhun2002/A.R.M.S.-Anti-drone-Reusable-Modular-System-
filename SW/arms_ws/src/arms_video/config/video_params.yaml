usb_cam_node_exe:
  ros__parameters:
    video_device: "/dev/video0"
    image_width: 1280
    image_height: 720
    framerate: 30.0
    pixel_format: "yuyv"       # or "mjpeg" depending on capture card
    camera_name: "fpv_camera"
    camera_info_url: ""
    # Topic will be remapped to /arms/image_raw in the launch file
